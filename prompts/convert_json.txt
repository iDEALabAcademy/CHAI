{add_error}
I would like you to reformat the code you just generated into a JSON object. The JSON should contain the following information:

    1. approxmated_code: The full block of code that you generated. Must be a single string with newlines escaped as \n.
    2. knob_variables: A string representation of a list of all the knob variable names. Example: "['loop_skip', 'threshold']". If no knob variables, return "[]".
    3. knob_ranges: A string representation of the ranges for each knob variable. Example: "[{{'loop_skip': [1, 5]}}, {{'threshold': [8, 20]}}]". If no knob variables, return "[]".
    4. knob_increments: A string representation of the step type for each knob. Example: "[{{'loop_skip': 'Integer'}}, {{'threshold': 'Real'}}]". If no knob variables, return "[]".

IMPORTANT: The JSON field name is "approxmated_code" (without the 'i' in approximated). All knob fields must be strings, not arrays.

Return ONLY a valid JSON object with double-quoted keys and string values. Do NOT wrap it in markdown code fences. Example:
{{
  "approxmated_code": "int foo() {{\n  return 0;\n}}",
  "knob_variables": "['x']",
  "knob_ranges": "[{{'x': [1, 10]}}]",
  "knob_increments": "[{{'x': 'Integer'}}]"
}}

{output_instuctions}