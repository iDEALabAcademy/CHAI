[
    {
        "functionName": "sobel_filtering",
        "completeFunction": "void sobel_filtering(void) {\n    /*Knob Variables Declaration Start*/\n    float loop_perforation_factor = 1.0; // Range: [0.1, 1.0], Real\n    /*Knob Variables Declaration End*/\n\n    int perforated_y_size1 = y_size1 * loop_perforation_factor;\n    int perforated_x_size1 = x_size1 * loop_perforation_factor;\n\n    for (int y = 0; y < perforated_y_size1; y++) {\n        for (int x = 0; x < perforated_x_size1; x++) {\n            input_buf[y * x_size1 + x] = image1[y][x];\n        }\n    }\n\n    sobel(input_buf, output_buf, x_size1, y_size1);\n\n#ifdef LOCAL_RUN\n    int perforated_y_size2 = y_size1 * loop_perforation_factor;\n    int perforated_x_size2 = x_size1 * loop_perforation_factor;\n\n    x_size2 = x_size1;\n    y_size2 = y_size1;\n\n    for (int y = 0; y < perforated_y_size2; y++) {\n        for (int x = 0; x < perforated_x_size2; x++) {\n            image2[y][x] = output_buf[y * x_size2 + x];\n        }\n    }\n#endif\n}",
        "knobVariables": "['loop_perforation_factor']",
        "knobRanges": "[{'loop_perforation_factor': [0.1, 1.0]}]",
        "knobStepSize": "[{'loop_perforation_factor': 'Real'}]",
        "filePath": "target/sobel-esp.c"
    },
    {
        "functionName": "sobel",
        "completeFunction": "void sobel(const uint8_t *input, uint8_t *output, int width, int height)\n{\n    /*Knob Variables Declaration Start*/\n    float loop_perforation_factor = 1.0; // Range: [0.1, 1.0], Real\n    int early_exit_threshold = 0;        // Range: [0, 255], Integer\n    int quantize_enabled = 0;            // Range: [0, 1], Integer\n    int shift_bits = 0;                  // Range: [0, 4], Integer\n    /*Knob Variables Declaration End*/\n\n    int perforated_height = height * loop_perforation_factor;\n    int perforated_width = width * loop_perforation_factor;\n\n    for (int y = 0; y < perforated_height; y++) {\n        for (int x = 0; x < perforated_width; x++) {\n            if (x == 0 || x == width - 1 || y == 0 || y == height - 1) {\n                output[y * width + x] = 0;\n                continue;\n            }\n\n            // Quantize input values if enabled\n            int p00 = input[(y - 1) * width + (x - 1)];\n            int p01 = input[(y - 1) * width + x];\n            int p02 = input[(y - 1) * width + (x + 1)];\n            int p10 = input[y * width + (x - 1)];\n            int p12 = input[y * width + (x + 1)];\n            int p20 = input[(y + 1) * width + (x - 1)];\n            int p21 = input[(y + 1) * width + x];\n            int p22 = input[(y + 1) * width + (x + 1)];\n\n            if (quantize_enabled) {\n                p00 = p00 >> shift_bits;\n                p01 = p01 >> shift_bits;\n                p02 = p02 >> shift_bits;\n                p10 = p10 >> shift_bits;\n                p12 = p12 >> shift_bits;\n                p20 = p20 >> shift_bits;\n                p21 = p21 >> shift_bits;\n                p22 = p22 >> shift_bits;\n            }\n\n            int gx = -p00 + p02 - 2 * p10 + 2 * p12 - p20 + p22;\n            int gy = p00 + 2 * p01 + p02 - p20 - 2 * p21 - p22;\n            int mag = abs(gx) + abs(gy);\n\n            // Early exit approximation\n            if (early_exit_threshold > 0 && mag < early_exit_threshold) {\n                output[y * width + x] = 0;\n                continue;\n            }\n\n            if (mag > 255) mag = 255;\n            output[y * width + x] = (uint8_t)mag;\n        }\n    }\n}",
        "knobVariables": "['loop_perforation_factor', 'early_exit_threshold', 'quantize_enabled', 'shift_bits']",
        "knobRanges": "[{'loop_perforation_factor': [0.1, 1.0]}, {'early_exit_threshold': [0, 255]}, {'quantize_enabled': [0, 1]}, {'shift_bits': [0, 4]}]",
        "knobStepSize": "[{'loop_perforation_factor': 'Real'}, {'early_exit_threshold': 'Integer'}, {'quantize_enabled': 'Integer'}, {'shift_bits': 'Integer'}]",
        "filePath": "target/sobel-esp.c"
    }
]